$id: "https://csr.sh/schemas/requests/SignupRequest"
title: Signup Request
type: object
description: Minimal payload for the example signup workflow.
properties:
  someData:
    type: string
    description: Just some data for the signup request

required:
  - someData
additionalProperties: false
example:
  someData: demo payload
